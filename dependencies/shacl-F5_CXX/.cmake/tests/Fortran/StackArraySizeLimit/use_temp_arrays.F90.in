program use_standard_semantics
  use, intrinsic :: iso_fortran_env
  write(output_unit,'(a)',advance='no') "$<JOIN:$<TARGET_PROPERTY:use_temp_arrays,COMPILE_OPTIONS>,"
  write(output_unit,'(a)',advance='no') " >"
end program
