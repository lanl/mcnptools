program main
  use, intrinsic :: iso_fortran_env, only: output_unit
  write(output_unit,'(a)',advance='no') "$<JOIN:$<TARGET_PROPERTY:fortran_standard_adherence,COMPILE_OPTIONS>,"
  write(output_unit,'(a)',advance='no') " >"
end program
